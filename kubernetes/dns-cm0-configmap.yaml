apiVersion: v1
data:
  named.conf: |-
    include "/etc/bind/named.conf.options";
    include "/etc/bind/named.conf.local";
kind: ConfigMap
metadata:
  annotations:
    use-subpath: "true"
  labels:
    io.kompose.service: dns
  name: dns-cm0
