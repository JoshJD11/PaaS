chmod +x generate_certs.sh   # da permiso de ejecución al script
./generate_certs.sh           # genera los 5 archivos de certificados
docker compose up -d          # levanta los 3 contenedores en background (-d = detached)
docker compose ps             # muestra el estado de cada contenedor
docker compose logs -f        # muestra los logs en tiempo real (útil para depurar)
docker compose down           # apaga y borra los contenedores (los volúmenes quedan)

docker compose exec db mysql -u wp_user -p  # Para entrar a la base de datos
Si pide contraseña, poner wp_password

http://localhost:8080   # Para ir a la pantalla de intalación de Wordpress para que se conecta a la base de datos y crear sus tablas automáticamente
Personalmente, puse:
Título del sitio: PaaS TEC (o el nombre del dominio si ya se tiene uno definido)
Nombre de usuario: admin (o algo fácil de recordar)
Contraseña: Admin1234! (no es segura pero es para recordar fácilmente)
Correo electrónico: admin@paas.tec.cr (En realidad puede ser cualquier correo, hasta uno inventado)
En cuanto a visibilidad de motores de búsqueda, no importa para el proyecto por lo que se puede dejar como está.

# ─────────────────────────────────────────
# OwnCloud — Levantar y probar
# ─────────────────────────────────────────
docker compose up -d owncloud_db owncloud_redis owncloud

docker compose logs -f owncloud
# Esperar a ver algo como "Initializing finished" o que el contenedor
# quede healthy (docker compose ps debe mostrar "healthy")

http://localhost:8082   # Pantalla de login de OwnCloud
Usuario: admin
Contraseña: Admin1234!

# Para comprobar que los archivos persisten en el volumen:
docker compose exec owncloud ls -la /mnt/data/files/admin/files

# Si necesitás reiniciar todo desde cero (borra archivos y BD de OwnCloud):
docker compose down
docker volume rm paas-ariana_owncloud_db_data paas-ariana_owncloud_redis_data paas-ariana_owncloud_files

# ─────────────────────────────────────────
# DHCP — Probar que el servidor reparte IPs
# ─────────────────────────────────────────
docker compose up -d dhcp dhcp_client_demo   # levanta solo el DHCP y un cliente de prueba

docker compose logs -f dhcp_client_demo
# Deberías ver algo así:
#   --- IP asignada por Docker al iniciar ---     -> ej. 172.30.0.3 (la dio el IPAM de Docker)
#   --- Solicitando IP real por DHCP ---           -> udhcpc manda DISCOVER/REQUEST
#   --- IP obtenida del servidor DHCP ---          -> ej. 172.30.0.1xx (la dio dhcpd, NO Docker)

docker compose logs dhcp
# Acá se ven los DHCPDISCOVER / DHCPOFFER / DHCPREQUEST / DHCPACK que procesó el server

# Para ver el handshake completo a nivel de paquetes (opcional, para la defensa):
docker compose exec dhcp tcpdump -i eth0 -n port 67 or port 68

# Para ver las concesiones (leases) otorgadas:
docker compose exec dhcp cat /var/lib/dhcp/dhcpd.leases

# ─────────────────────────────────────────
# Thin Client Server (LTSP) — Levantar y probar
# ─────────────────────────────────────────

docker compose up -d --build ltsp_server    # docker compose up -d --build dhcp ltsp_server para iniciar el dhcp de una vez
# El build puede tardar varios minutos la primera vez (instala un
# kernel real de Ubuntu para extraer vmlinuz, y arma el initrd/rootfs).

docker compose logs -f ltsp_server
# Se debería ver: "Iniciando rpc.mountd y rpc.nfsd", luego "Iniciando TFTP..."

# Paso 1: smoke test (NO hace PXE real, solo valida que el server responde)
docker compose --profile test up ltsp_smoke_test
# Si todo sale bien verás el contenido del rootfs (busybox, init, etc.)
# montado por NFS, y la descarga exitosa de pxelinux.0 por TFTP.

# Paso 2: prueba real de arranque PXE con una VM (QEMU), por fuera
# del docker-compose (Docker no puede emular firmware/BIOS PXE).
# Necesitas la VM/host donde corre Docker con acceso a "ip" y "qemu-system-x86_64".

# 2.1 Encontrar el bridge de Linux que Docker creó para la red "wan":
docker network inspect paas-ariana_wan -f '{{.Id}}'
# Toma los primeros 12 caracteres del Id, el bridge se llama "br-<esos 12 caracteres>"
ip link show | grep br-

# 2.2 Crear una interfaz tap y conectarla a ese bridge:
sudo ip tuntap add dev tap0 mode tap
sudo ip link set tap0 master br-XXXXXXXXXXXX   # sustituí por el bridge real
sudo ip link set tap0 up

# 2.3 Levantar la VM en modo "network boot" (-boot n):
sudo qemu-system-x86_64 \
  -m 512 \
  -boot n \
  -netdev tap,id=net0,ifname=tap0,script=no,downscript=no \
  -device e1000,netdev=net0 \
  -nographic

# Deberías ver en la consola de QEMU: el menú PXELINUX, luego el kernel
# cargando, "PaaS.net Thin Client: solicitando IP por DHCP...", y al final
# el banner del rootfs montado por NFS con una shell de BusyBox.

# Para limpiar la interfaz tap luego de la prueba:
sudo ip link delete tap0
